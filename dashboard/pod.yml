apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
  - name: dashboard
    image: registry.gitlab.com/kfs-mining/dashboard/dashboard:latest
  imagePullSecrets:
    - name: gitlab-auth
